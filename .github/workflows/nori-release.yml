# Nori Release Workflow
#
# This workflow builds and publishes nori-ai-cli releases to npm.
# It is triggered by tag pushes created via the create_nori_release script.
#
# The script creates "synthetic commits" - release tags point to commits that
# exist only for the release (not on any branch), with Cargo.toml updated to
# the release version. This keeps the dev branch's Cargo.toml at a placeholder.
#
# Usage:
#   # Create a release (from local machine with gh CLI authenticated)
#   ./scripts/create_nori_release --publish-release      # stable release
#   ./scripts/create_nori_release --publish-alpha        # alpha release
#   ./scripts/create_nori_release --publish-next         # dev snapshot for testing
#   ./scripts/create_nori_release --version 0.3.0        # specific version
#   ./scripts/create_nori_release --dry-run --publish-release  # preview
#
# The script pushes the tag, which triggers this workflow automatically.
#
# For testing the build process without releasing:
#   gh workflow run nori-release.yml -f version=0.2.0 -f dry_run=true
#   gh workflow run nori-release.yml -f version=0.2.0 -f dry_run=true -f skip_tests=true
#
# For publishing a dev snapshot directly from GitHub UI:
#   gh workflow run nori-release.yml -f publish_next=true
#
name: Nori Release

on:
  push:
    tags:
      - "nori-v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build (e.g., 0.2.0) - ignored if publish_next is true"
        required: false
        type: string
      publish_next:
        description: "Publish a dev snapshot (@next) - auto-determines version"
        required: false
        default: false
        type: boolean
      dry_run:
        description: "Dry run mode (builds but does not publish)"
        required: false
        default: true
        type: boolean
      skip_tests:
        description: "Skip running tests (for faster build testing)"
        required: false
        default: false
        type: boolean

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"
  NPM_PACKAGE_NAME: "nori-ai-cli"
  TAG_PREFIX: "nori-v"

jobs:
  # ============================================================================
  # VALIDATION
  # ============================================================================
  validate:
    name: Validate Release
    runs-on: blacksmith-4vcpu-ubuntu-2404
    outputs:
      version: ${{ steps.validate.outputs.version }}
      tag_name: ${{ steps.validate.outputs.tag_name }}
      is_prerelease: ${{ steps.validate.outputs.is_prerelease }}
      is_tag_push: ${{ steps.validate.outputs.is_tag_push }}
      publish_next: ${{ steps.validate.outputs.publish_next }}
      checkout_ref: ${{ steps.validate.outputs.checkout_ref }}
      skip_tests: ${{ steps.validate.outputs.skip_tests }}
    steps:
      - name: Validate version and determine refs
        id: validate
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          IS_TAG_PUSH="false"
          PUBLISH_NEXT="false"

          if [[ "${{ github.event_name }}" == "push" ]]; then
            IS_TAG_PUSH="true"
            # Extract version from tag name (nori-v0.2.0 -> 0.2.0)
            VERSION="${GITHUB_REF_NAME#${{ env.TAG_PREFIX }}}"
            TAG_NAME="${GITHUB_REF_NAME}"
            CHECKOUT_REF="${GITHUB_REF}"
          elif [[ "${{ inputs.publish_next }}" == "true" ]]; then
            PUBLISH_NEXT="true"
            # Auto-determine the next -next.N version
            # NOTE: This logic mirrors scripts/create_nori_release determine_version()
            # Keep them in sync if changes are made
            echo "Determining next dev snapshot version..."

            # Get the latest stable release version
            LATEST_STABLE=$(gh api repos/${{ github.repository }}/releases/latest --jq '.tag_name' 2>/dev/null || echo "")
            if [[ -z "$LATEST_STABLE" || "$LATEST_STABLE" == "null" ]]; then
              # No releases yet, use 0.0.0 as base
              BASE_VERSION="0.0.0"
            else
              # Strip the tag prefix to get version
              BASE_VERSION="${LATEST_STABLE#${{ env.TAG_PREFIX }}}"
              # Strip any prerelease suffix to get the stable base
              BASE_VERSION="${BASE_VERSION%%-*}"
            fi

            echo "Base stable version: $BASE_VERSION"

            # Find the highest existing -next.N for this base version
            NEXT_PREFIX="${BASE_VERSION}-next."
            HIGHEST_NEXT=0

            # List all releases and find -next versions
            RELEASES=$(gh api repos/${{ github.repository }}/releases --paginate --jq '.[].tag_name' 2>/dev/null || echo "")
            while IFS= read -r tag; do
              if [[ "$tag" == "${{ env.TAG_PREFIX }}${NEXT_PREFIX}"* ]]; then
                SUFFIX="${tag#${{ env.TAG_PREFIX }}${NEXT_PREFIX}}"
                if [[ "$SUFFIX" =~ ^[0-9]+$ ]]; then
                  if (( SUFFIX > HIGHEST_NEXT )); then
                    HIGHEST_NEXT=$SUFFIX
                  fi
                fi
              fi
            done <<< "$RELEASES"

            VERSION="${NEXT_PREFIX}$((HIGHEST_NEXT + 1))"
            TAG_NAME="${{ env.TAG_PREFIX }}${VERSION}"
            CHECKOUT_REF="dev"

            echo "Auto-determined version: $VERSION"
          else
            VERSION="${{ inputs.version }}"
            if [[ -z "$VERSION" ]]; then
              echo "::error::Must specify either 'version' or 'publish_next=true'"
              exit 1
            fi
            TAG_NAME="${{ env.TAG_PREFIX }}${VERSION}"
            # For dispatch dry run, checkout dev branch
            CHECKOUT_REF="dev"
          fi

          echo "is_tag_push=$IS_TAG_PUSH" >> "$GITHUB_OUTPUT"
          echo "publish_next=$PUBLISH_NEXT" >> "$GITHUB_OUTPUT"
          echo "checkout_ref=$CHECKOUT_REF" >> "$GITHUB_OUTPUT"

          # Validate semver format (X.Y.Z or X.Y.Z-alpha.N, X.Y.Z-beta.N, X.Y.Z-next.N, etc.)
          if ! echo "$VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+(-[a-zA-Z]+\.[0-9]+)?$'; then
            echo "::error::Invalid version format: $VERSION"
            echo "Expected format: X.Y.Z or X.Y.Z-alpha.N"
            exit 1
          fi

          # Determine if this is a prerelease
          IS_PRERELEASE="false"
          if echo "$VERSION" | grep -qE '-'; then
            IS_PRERELEASE="true"
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag_name=$TAG_NAME" >> "$GITHUB_OUTPUT"
          echo "is_prerelease=$IS_PRERELEASE" >> "$GITHUB_OUTPUT"

          # Determine if tests should be skipped (only for workflow_dispatch)
          SKIP_TESTS="false"
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.skip_tests }}" == "true" ]]; then
            SKIP_TESTS="true"
          fi
          echo "skip_tests=$SKIP_TESTS" >> "$GITHUB_OUTPUT"

          # Determine run mode
          RUN_MODE="Dry Run (build test only)"
          if [[ "$IS_TAG_PUSH" == "true" ]]; then
            RUN_MODE="Release (tag push)"
          elif [[ "$PUBLISH_NEXT" == "true" && "${{ inputs.dry_run }}" != "true" ]]; then
            RUN_MODE="Publish @next"
          fi

          echo "## Release Validation" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Property | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|----------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | \`$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`$TAG_NAME\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Pre-release | $IS_PRERELEASE |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Trigger | ${{ github.event_name }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Checkout Ref | \`$CHECKOUT_REF\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Mode | $RUN_MODE |" >> "$GITHUB_STEP_SUMMARY"
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "| Skip Tests | $SKIP_TESTS |" >> "$GITHUB_STEP_SUMMARY"
          fi

  # ============================================================================
  # RUN TESTS
  # ============================================================================
  test:
    name: Run Tests
    needs: validate
    if: ${{ needs.validate.outputs.skip_tests != 'true' }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    env:
      MUSL_TARGET: x86_64-unknown-linux-musl
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate.outputs.checkout_ref }}

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android || true
          df -h

      - name: Install musl tools
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.90.0"
          targets: ${{ env.MUSL_TARGET }}
          components: clippy

      - name: Setup faster linker
        uses: rui314/setup-mold@v1

      - name: cargo check
        working-directory: codex-rs
        run: cargo check --profile ci-test --target ${{ env.MUSL_TARGET }} --all-targets

      # - name: cargo clippy
      #   working-directory: codex-rs
      #   run: cargo clippy --target ${{ env.MUSL_TARGET }} --all-features -- -D warnings

      # - name: cargo build
      #   working-directory: codex-rs
      #   run: |
      #     cargo build --profile ci-test --target ${{ env.MUSL_TARGET }} -p codex-cli
      #     cargo build --profile ci-test --target ${{ env.MUSL_TARGET }} -p mock-acp-agent

      # - name: cargo test
      #   working-directory: codex-rs
      #   run: |
      #     cargo test --profile ci-test --target ${{ env.MUSL_TARGET }} --package tui-pty-e2e
      #     cargo test --profile ci-test --target ${{ env.MUSL_TARGET }} --package codex-acp
      #     cargo test --profile ci-test --target ${{ env.MUSL_TARGET }} --package codex-tui
      #     cargo test --profile ci-test --target ${{ env.MUSL_TARGET }} --package codex-cli
      #     cargo test --profile ci-test --target ${{ env.MUSL_TARGET }} --package codex-protocol

  # ============================================================================
  # BUILD NATIVE BINARIES
  # ============================================================================
  build-native:
    name: Build Native (${{ matrix.target }})
    needs: [validate, test]
    if: ${{ !failure() && !cancelled() }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: blacksmith-4vcpu-ubuntu-2404
            target: x86_64-unknown-linux-musl
            artifact-name: linux-x86_64
          - os: blacksmith-4vcpu-ubuntu-2404-arm
            target: aarch64-unknown-linux-musl
            artifact-name: linux-arm64
          - os: macos-14
            target: aarch64-apple-darwin
            artifact-name: darwin-arm64
          - os: macos-14
            target: x86_64-apple-darwin
            artifact-name: darwin-x86_64
          # Android targets temporarily disabled - OpenSSL cross-compilation needs more work
          # - os: ubuntu-24.04
          #   target: aarch64-linux-android
          #   artifact-name: android-arm64
          #   use-ndk: true
          # - os: ubuntu-24.04
          #   target: x86_64-linux-android
          #   artifact-name: android-x86_64
          #   use-ndk: true
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate.outputs.checkout_ref }}

      - name: Free disk space (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc || true
          # Only delete Android SDK if not building for Android (NDK setup needs it)
          if [[ "${{ matrix.use-ndk }}" != "true" ]]; then
            sudo rm -rf /usr/local/lib/android || true
          fi
          df -h

      - name: Install musl tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Setup faster linker
        uses: rui314/setup-mold@v1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.90.0"
          targets: ${{ matrix.target }}

      - name: Setup Android NDK
        if: matrix.use-ndk == true
        uses: android-actions/setup-android@v3
        with:
          packages: "ndk;26.1.10909125"

      - name: Configure Android toolchain
        if: matrix.use-ndk == true
        run: |
          NDK_HOST_TAG="linux-x86_64"
          NDK_TOOLCHAIN="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/$NDK_HOST_TAG/bin"

          # Set CC and AR for native crate builds (ring, etc.)
          echo "CC_aarch64_linux_android=$NDK_TOOLCHAIN/aarch64-linux-android30-clang" >> $GITHUB_ENV
          echo "AR_aarch64_linux_android=$NDK_TOOLCHAIN/llvm-ar" >> $GITHUB_ENV
          echo "CC_x86_64_linux_android=$NDK_TOOLCHAIN/x86_64-linux-android30-clang" >> $GITHUB_ENV
          echo "AR_x86_64_linux_android=$NDK_TOOLCHAIN/llvm-ar" >> $GITHUB_ENV

          # Create symlinks for cross-compilation tools that openssl-src expects
          # OpenSSL build scripts look for <target>-ranlib, <target>-ar, etc.
          ln -sf "$NDK_TOOLCHAIN/llvm-ranlib" "$NDK_TOOLCHAIN/aarch64-linux-android-ranlib"
          ln -sf "$NDK_TOOLCHAIN/llvm-ranlib" "$NDK_TOOLCHAIN/x86_64-linux-android-ranlib"
          ln -sf "$NDK_TOOLCHAIN/llvm-ar" "$NDK_TOOLCHAIN/aarch64-linux-android-ar"
          ln -sf "$NDK_TOOLCHAIN/llvm-ar" "$NDK_TOOLCHAIN/x86_64-linux-android-ar"

          # Add NDK toolchain to PATH so openssl-src can find the tools
          echo "$NDK_TOOLCHAIN" >> $GITHUB_PATH

          # Configure linker in cargo config
          mkdir -p ~/.cargo
          cat >> ~/.cargo/config.toml << EOF
          [target.aarch64-linux-android]
          linker = "$NDK_TOOLCHAIN/aarch64-linux-android30-clang"

          [target.x86_64-linux-android]
          linker = "$NDK_TOOLCHAIN/x86_64-linux-android30-clang"
          EOF
          cat ~/.cargo/config.toml

      - name: Build release binaries
        working-directory: codex-rs
        run: |
          cargo build --release --target ${{ matrix.target }} -p codex-cli

          # List built artifacts
          ls -la target/${{ matrix.target }}/release/

      - name: Prepare artifacts
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          TARGET="${{ matrix.target }}"
          ARTIFACT_NAME="${{ matrix.artifact-name }}"

          mkdir -p artifacts/vendor/$TARGET/nori

          # Copy the main binary
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            cp codex-rs/target/$TARGET/release/nori.exe artifacts/vendor/$TARGET/nori/nori.exe
          else
            cp codex-rs/target/$TARGET/release/nori artifacts/vendor/$TARGET/nori/nori
            chmod +x artifacts/vendor/$TARGET/nori/nori
          fi

          # Create artifact info
          echo "$VERSION" > artifacts/VERSION
          echo "$TARGET" > artifacts/TARGET

          ls -la artifacts/vendor/$TARGET/nori/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: native-${{ matrix.artifact-name }}
          path: artifacts/
          retention-days: 7

  # ============================================================================
  # STAGE NPM PACKAGES
  # ============================================================================
  stage-npm:
    name: Stage npm Package
    needs: [validate, build-native]
    if: ${{ !failure() && !cancelled() }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate.outputs.checkout_ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.8.1

      - name: Download all native artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: native-*
          path: native-artifacts/
          merge-multiple: false

      - name: Organize vendor directory
        run: |
          mkdir -p vendor

          # Merge all platform artifacts into vendor directory
          for artifact_dir in native-artifacts/native-*/; do
            if [[ -d "$artifact_dir/vendor" ]]; then
              cp -r "$artifact_dir/vendor/"* vendor/
            fi
          done

          echo "## Vendor Directory Contents" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          find vendor -type f | head -20 >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Restore binary permissions
        run: |
          # GitHub Actions artifacts don't preserve Unix file permissions
          # Restore execute permission on all nori binaries
          find vendor -type f -name "nori" ! -name "*.exe" -exec chmod +x {} \;

      - name: Create npm package
        run: |
          # Install tree for directory visualization
          sudo apt-get update && sudo apt-get install -y tree

          VERSION="${{ needs.validate.outputs.version }}"
          PACKAGE_NAME="${{ env.NPM_PACKAGE_NAME }}"

          mkdir -p dist/npm

          # Create the package directory
          STAGING_DIR="dist/staging"
          mkdir -p "$STAGING_DIR/bin"
          mkdir -p "$STAGING_DIR/vendor"

          # Copy the CLI launcher and platform module
          cp codex-cli/bin/nori.js "$STAGING_DIR/bin/"
          cp codex-cli/bin/platform.js "$STAGING_DIR/bin/"

          # Copy vendor binaries
          cp -r vendor/* "$STAGING_DIR/vendor/"

          # Copy README
          cp README.md "$STAGING_DIR/" 2>/dev/null || echo "No README.md found"

          # Copy package.json and update version for release
          cp codex-cli/package.json "$STAGING_DIR/package.json"
          cd "$STAGING_DIR"
          npm version "$VERSION" --no-git-tag-version --allow-same-version
          cd -

          # Show staging directory contents
          echo "## Staging Directory Contents" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          tree "$STAGING_DIR" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

          # Pack the package
          cd "$STAGING_DIR"
          npm pack --pack-destination ../npm

          # List the created package
          echo "## Created npm Package" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          ls -la ../npm/ >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Upload npm package artifact
        uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: dist/npm/*.tgz
          retention-days: 7

  # ============================================================================
  # CREATE TAG FOR PUBLISH_NEXT (only for workflow_dispatch with publish_next=true and dry_run=false)
  # ============================================================================
  create-next-tag:
    name: Create @next Tag
    needs: [validate, stage-npm]
    # Only run when publish_next=true and dry_run=false (not for tag pushes)
    if: ${{ !failure() && !cancelled() && needs.validate.outputs.publish_next == 'true' && inputs.dry_run == false }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: dev

      - name: Create synthetic commit and tag
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          TAG_NAME="${{ needs.validate.outputs.tag_name }}"
          CARGO_TOML_PATH="codex-rs/Cargo.toml"

          echo "Creating synthetic commit for version $VERSION"

          # Get the dev branch head SHA
          BASE_COMMIT=$(gh api repos/${{ github.repository }}/git/refs/heads/dev --jq '.object.sha')
          echo "Base commit: $BASE_COMMIT"

          # Get the tree SHA for the base commit
          BASE_TREE=$(gh api repos/${{ github.repository }}/git/commits/$BASE_COMMIT --jq '.tree.sha')
          echo "Base tree: $BASE_TREE"

          # Fetch current Cargo.toml contents
          CARGO_CONTENT=$(gh api repos/${{ github.repository }}/contents/$CARGO_TOML_PATH?ref=$BASE_COMMIT --jq '.content' | base64 -d)

          # Update the version in Cargo.toml
          UPDATED_CONTENT=$(echo "$CARGO_CONTENT" | sed "0,/^version = \"[^\"]*\"/s//version = \"$VERSION\"/")

          # Create a blob with the updated content
          BLOB_SHA=$(echo "$UPDATED_CONTENT" | gh api repos/${{ github.repository }}/git/blobs -X POST -f content=@- -f encoding=utf-8 --jq '.sha')
          echo "Blob SHA: $BLOB_SHA"

          # Create a new tree with the updated file
          TREE_SHA=$(gh api repos/${{ github.repository }}/git/trees -X POST \
            -f base_tree="$BASE_TREE" \
            -f "tree[][path]=$CARGO_TOML_PATH" \
            -f "tree[][mode]=100644" \
            -f "tree[][type]=blob" \
            -f "tree[][sha]=$BLOB_SHA" \
            --jq '.sha')
          echo "Tree SHA: $TREE_SHA"

          # Create a commit
          COMMIT_SHA=$(gh api repos/${{ github.repository }}/git/commits -X POST \
            -f message="Release $VERSION" \
            -f tree="$TREE_SHA" \
            -f "parents[]=$BASE_COMMIT" \
            --jq '.sha')
          echo "Commit SHA: $COMMIT_SHA"

          # Create an annotated tag
          TAG_SHA=$(gh api repos/${{ github.repository }}/git/tags -X POST \
            -f tag="$TAG_NAME" \
            -f message="Nori release $VERSION" \
            -f object="$COMMIT_SHA" \
            -f type="commit" \
            --jq '.sha')
          echo "Tag SHA: $TAG_SHA"

          # Create the tag ref (check if exists first for idempotency on retry)
          if gh api repos/${{ github.repository }}/git/refs/tags/$TAG_NAME 2>/dev/null; then
            echo "Tag $TAG_NAME already exists, skipping creation"
          else
            gh api repos/${{ github.repository }}/git/refs -X POST \
              -f ref="refs/tags/$TAG_NAME" \
              -f sha="$TAG_SHA"
          fi

          echo "## Tag Created" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Property | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|----------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | \`$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`$TAG_NAME\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | \`$COMMIT_SHA\` |" >> "$GITHUB_STEP_SUMMARY"

  # ============================================================================
  # PUBLISH TO NPM (only for tag push, not dry run)
  # ============================================================================
  publish-npm:
    name: Publish to npm
    needs: [validate, stage-npm, create-next-tag]
    # Publish for tag pushes OR when publish_next=true with dry_run=false
    # Note: create-next-tag is skipped for tag pushes but we still need the dependency for ordering
    if: ${{ !failure() && !cancelled() && (github.event_name == 'push' || (needs.validate.outputs.publish_next == 'true' && inputs.dry_run == false)) }}
    runs-on: ubuntu-24.04
    environment: npm-publish
    permissions:
      contents: read # Required to checkout code
      id-token: write # Required for OIDC Trusted Publishing
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate.outputs.checkout_ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Download npm package
        uses: actions/download-artifact@v4
        with:
          name: npm-package
          path: dist/

      - name: Check if version already published
        id: check-published
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          PACKAGE_NAME="${{ env.NPM_PACKAGE_NAME }}"

          # Check if this version is already published
          if npm view "$PACKAGE_NAME@$VERSION" version 2>/dev/null; then
            echo "::error::Version $VERSION is already published to npm"
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            exit 1
          else
            echo "Version $VERSION is not yet published"
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          IS_PRERELEASE="${{ needs.validate.outputs.is_prerelease }}"

          # Find the tarball - use ./ prefix to force local path resolution
          TARBALL="./$(ls dist/*.tgz | head -1)"

          if [[ -z "$TARBALL" || "$TARBALL" == "./" ]]; then
            echo "::error::No tarball found in dist/"
            exit 1
          fi

          echo "Publishing $TARBALL"

          # Determine npm tag
          NPM_TAG="latest"
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            NPM_TAG="next"
          fi

          # Publish with OIDC Trusted Publishing (no --provenance for private repos)
          npm publish "$TARBALL" --tag "$NPM_TAG" --access public

          echo "## npm Publish Complete" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Published \`${{ env.NPM_PACKAGE_NAME }}@$VERSION\` with tag \`$NPM_TAG\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Install with:" >> "$GITHUB_STEP_SUMMARY"
          echo '```bash' >> "$GITHUB_STEP_SUMMARY"
          echo "npm install -g ${{ env.NPM_PACKAGE_NAME }}@$VERSION" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Update @next tag to point to latest
        if: ${{ needs.validate.outputs.is_prerelease != 'true' }}
        run: |
          npm dist-tag add "${{ env.NPM_PACKAGE_NAME }}@${{ needs.validate.outputs.version }}" next
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Also updated \`@next\` tag to point to \`${{ needs.validate.outputs.version }}\`" >> "$GITHUB_STEP_SUMMARY"

  # ============================================================================
  # CREATE GITHUB RELEASE (only for tag push)
  # ============================================================================
  create-release:
    name: Create GitHub Release
    needs: [validate, build-native, publish-npm, create-next-tag]
    # Create release for tag pushes OR when publish_next=true with dry_run=false
    # Note: create-next-tag is skipped for tag pushes but we still need the dependency for ordering
    if: ${{ !failure() && !cancelled() && (github.event_name == 'push' || (needs.validate.outputs.publish_next == 'true' && inputs.dry_run == false)) }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          # For tag pushes use the tag, for publish_next use the tag that was just created
          ref: ${{ needs.validate.outputs.tag_name }}
          fetch-depth: 0

      - name: Download all native artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: native-*
          path: release-artifacts/

      - name: Download npm package
        uses: actions/download-artifact@v4
        with:
          name: npm-package
          path: release-artifacts/npm/

      - name: Prepare release assets
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          mkdir -p release-assets

          # Create platform-specific archives
          for artifact_dir in release-artifacts/native-*/; do
            PLATFORM=$(basename "$artifact_dir" | sed 's/native-//')

            if [[ -d "$artifact_dir/vendor" ]]; then
              # Find the target directory
              for target_dir in "$artifact_dir/vendor/"*/; do
                TARGET=$(basename "$target_dir")

                # Create archive
                ARCHIVE_NAME="nori-${VERSION}-${PLATFORM}.tar.gz"
                tar -czvf "release-assets/$ARCHIVE_NAME" -C "$target_dir" .

                echo "Created: $ARCHIVE_NAME"
              done
            fi
          done

          # Copy npm tarball
          cp release-artifacts/npm/*.tgz release-assets/ 2>/dev/null || true

          ls -la release-assets/

      - name: Generate changelog
        id: changelog
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          TAG_NAME="${{ needs.validate.outputs.tag_name }}"

          # Try to generate changelog with git-cliff if available
          if command -v git-cliff &> /dev/null; then
            git-cliff --latest --strip header > CHANGELOG_RELEASE.md
          else
            # Fallback: generate simple changelog from commits
            echo "## What's Changed" > CHANGELOG_RELEASE.md
            echo "" >> CHANGELOG_RELEASE.md

            # Get previous tag
            PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

            if [[ -n "$PREV_TAG" ]]; then
              git log --pretty=format:"* %s (%h)" "$PREV_TAG"..HEAD >> CHANGELOG_RELEASE.md
            else
              git log --pretty=format:"* %s (%h)" -20 >> CHANGELOG_RELEASE.md
            fi
          fi

          echo "" >> CHANGELOG_RELEASE.md
          echo "" >> CHANGELOG_RELEASE.md
          echo "## Installation" >> CHANGELOG_RELEASE.md
          echo "" >> CHANGELOG_RELEASE.md
          echo '```bash' >> CHANGELOG_RELEASE.md
          echo "npm install -g ${{ env.NPM_PACKAGE_NAME }}@$VERSION" >> CHANGELOG_RELEASE.md
          echo '```' >> CHANGELOG_RELEASE.md

          cat CHANGELOG_RELEASE.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate.outputs.tag_name }}
          name: "Nori ${{ needs.validate.outputs.version }}"
          body_path: CHANGELOG_RELEASE.md
          draft: false
          prerelease: ${{ needs.validate.outputs.is_prerelease == 'true' }}
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          TAG_NAME="${{ needs.validate.outputs.tag_name }}"

          echo "## Release Complete!" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | \`$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`$TAG_NAME\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| npm | \`${{ env.NPM_PACKAGE_NAME }}@$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| GitHub Release | [$TAG_NAME](https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME) |" >> "$GITHUB_STEP_SUMMARY"

  # ============================================================================
  # DRY RUN SUMMARY (only for workflow_dispatch with dry_run=true)
  # ============================================================================
  dry-run-summary:
    name: Dry Run Summary
    needs: [validate, stage-npm]
    # Only show dry run summary when dry_run is true (not when actually publishing)
    if: ${{ !failure() && !cancelled() && github.event_name == 'workflow_dispatch' && inputs.dry_run == true }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Download npm package
        uses: actions/download-artifact@v4
        with:
          name: npm-package
          path: dist/

      - name: Dry run summary
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          TAG_NAME="${{ needs.validate.outputs.tag_name }}"
          IS_PRERELEASE="${{ needs.validate.outputs.is_prerelease }}"
          PUBLISH_NEXT="${{ needs.validate.outputs.publish_next }}"

          echo "## Dry Run Complete" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Build test successful! The following would be created in a real release:" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Git Tag | \`$TAG_NAME\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| npm Package | \`${{ env.NPM_PACKAGE_NAME }}@$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| npm Tag | \`$([ \"$IS_PRERELEASE\" == \"true\" ] && echo \"next\" || echo \"latest\")\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Pre-release | $IS_PRERELEASE |" >> "$GITHUB_STEP_SUMMARY"
          if [[ "$PUBLISH_NEXT" == "true" ]]; then
            echo "| Mode | Dev Snapshot (@next) |" >> "$GITHUB_STEP_SUMMARY"
          fi
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Staged Package" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          ls -la dist/ >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "To create an actual release:" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [[ "$PUBLISH_NEXT" == "true" ]]; then
            echo "**Option 1: From GitHub UI**" >> "$GITHUB_STEP_SUMMARY"
            echo '```bash' >> "$GITHUB_STEP_SUMMARY"
            echo "gh workflow run nori-release.yml -f publish_next=true -f dry_run=false" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "**Option 2: Using the release script**" >> "$GITHUB_STEP_SUMMARY"
            echo '```bash' >> "$GITHUB_STEP_SUMMARY"
            echo "./scripts/create_nori_release --publish-next" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          else
            echo '```bash' >> "$GITHUB_STEP_SUMMARY"
            echo "./scripts/create_nori_release --version $VERSION" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi
