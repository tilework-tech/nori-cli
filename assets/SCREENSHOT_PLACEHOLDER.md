# Screenshot Placeholder

Add a TUI screenshot here named `screenshot.png`.

Then update the README.md to uncomment the image reference:

```markdown
![Nori TUI Screenshot](assets/screenshot.png)
```

Recommended screenshot specs:
- Show the TUI in action with a sample conversation
- Terminal size: ~120x30 characters
- Use a clean, dark terminal theme for contrast
- PNG format, optimized for web
